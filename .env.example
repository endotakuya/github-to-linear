# Linear API Key (optional if you use --linear-key CLI option)
# Get it from: https://linear.app/settings/api
LINEAR_API_KEY=lin_api_xxxxxxxxxxxxx

# Default Linear Team ID (optional)
# You can find it in the Linear URL or via API
LINEAR_TEAM_ID=

# Default priority (optional, 0-4: No priority, Urgent, High, Medium, Low)
DEFAULT_PRIORITY=3

# Note: GitHub authentication uses `gh` CLI command
# Make sure `gh` is installed and authenticated: gh auth login
#
# API Key Priority:
# 1. --linear-key CLI argument (highest priority)
# 2. LINEAR_API_KEY environment variable
